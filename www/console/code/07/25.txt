function Car(){
    var carName, carInfo;

    // create a car
	  function _make(name,id) {
	      returngetAjaxData(na)
	  }

    // get data from url
	  function getAjaxData(car,id) {

       // we get car data from some URL
       // carInfo will set here
       return carInfo;
	  }

	  return {
	      make: _make
	  };
}

// create a `Car` module instance
var car = Car();

car.make( 'peykan', 23 );
